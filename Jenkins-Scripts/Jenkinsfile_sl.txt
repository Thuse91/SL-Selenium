pipeline {
  
  agent any
  tools { nodejs "nodejs" }
  
  stages {
    stage("Tools initiated") {   
      steps {
        echo "npm -v"
        echo "git --version"
        echo "node -v"
      }
    }
    stage("Checkout Code") {
      steps {
        git branch: "main", url:"https://github.com/Thuse91/Test-Verktyg.git"
      }
    }
    stage("build") {
      steps {
        bat "install_scrip.bat"
        echo 'npm install --save selenium-webdriver chromedriver'
        echo 'npm install --save -g chromedriver'
        }
      }
    stage("Web GUI test") {
      steps {
        bat "run_script.bat"
        }
      }
    }
  }
