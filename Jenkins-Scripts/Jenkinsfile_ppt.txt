pipeline {
  
  agent any
  tools { nodejs "nodejs" }
  
  stages {
    stage("Tools initiated") {   
      steps {
        echo "npm -v"
        echo "git --version"
      }
    }
    stage("Checkout Code") {
      steps {
        git branch: "main", url:"https://github.com/Thuse91/Test-Verktyg.git"
      }
    }
    stage("build") {
      steps {
        bat "install_script.bat"
        echo 'npm -g install puppeteer'
        }
      }
    stage("Web GUI test") {
      steps {
        echo "node testPPT"
        }
      }
    }
  }
